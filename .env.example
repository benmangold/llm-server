# Copy this file to .env and adjust as needed:  cp .env.example .env
#
# Comma-separated list of Ollama models to pull on startup.
# The ollama-pull service reads this and pulls each into the shared volume.
# Examples: llama3.2, qwen2.5:3b, nomic-embed-text, phi3.5
OLLAMA_MODELS=llama3.2
